from main.models import UserProfile

def save_user(backend, djangoUser, response, *args, **kwargs):
	'''
	if kwargs['is_new']:
		original_user = kwargs['user']
		u = UserProfile(user = original_user, username
	'''
	pass
